server:
  port: 8095
  error:
    include-message: always

spring:
  application:
    name: servicio-centroservicio
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086
  h2:
    console: true
    datasource:
      url: jdbc:h2:mem:centroservicioDB;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      driverClassName: org.h2.Driver
      platform: h2
      username: sa
      password:
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
      database-platform: org.hibernate.dialect.H2Dialect
      properties:
        hibernate:
          show-sql: true
          use_sql_comments: false
          formate_sql: false
  logging:
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

